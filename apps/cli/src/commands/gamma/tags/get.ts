// Generated by Bao - DO NOT EDIT

import {
  argument,
  defineCommand,
  type InferArgs,
  type InferOptions,
  option,
} from "boune";
import { run } from "../../../handlers/gamma/tags/get.js";

const args = {
  id: argument.string().required().describe("Tag ID or slug"),
} as const;

const options = {
  includeTemplate: option
    .boolean()
    .short("t")
    .describe("Include template data"),
  bySlug: option.boolean().short("s").describe("Lookup by slug instead of ID"),
} as const;

export const getCommand = defineCommand({
  name: "get",
  description: "Get a specific tag by ID or slug",
  arguments: args,
  options: options,
  action: async ({ args, options }) => {
    await run(args, options);
  },
});

export type GetArgs = InferArgs<typeof args>;
export type GetOptions = InferOptions<typeof options>;
